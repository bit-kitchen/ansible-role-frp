#!/usr/bin/env ansible-playbook
---
- hosts: localhost
  gather_facts: yes
  roles:
    - name: ../../frps
      #frps_version: ""

      #frps_config_name: ''
      #frps_bind_addr: '0.0.0.0'
      #frps_bind_port: 7000
      #frps_token: ''
