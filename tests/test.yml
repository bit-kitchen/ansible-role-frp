#!/usr/bin/env ansible-playbook
---
- hosts: localhost
  gather_facts: yes
  roles:
    - name: ../..
      #version: "0.31.2"

      #install_frpc: yes
      #install_frps: yes
      #configure_frpc: no
      #configure_frps: no

      #server_addr: '127.0.0.1'
      #server_port: 7000
