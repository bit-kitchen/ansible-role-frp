[common]
server_addr = {{ server_addr }}
server_port = {{ server_port }}
{% if token is defined %}
token = {{ token }}
{% endif %}
{% if http_proxy is defined %}
http_proxy = {{ http_proxy }}
{% endif %}
